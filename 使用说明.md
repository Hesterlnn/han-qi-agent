# 韩琦智能体 V1.0 使用说明

这份说明只讲如何选择和使用韩琦智能体。即使不了解API、编程或Agent，也可以从“普通聊天框版”开始。

如果想直接体验，阅读“版本选择”和“普通聊天框版”即可；需要本地网页或Agent时，再阅读对应章节。

## 目录

### 第一部分：功能说明

1. [它可以做什么](#一它可以做什么)
2. [四种互动模式](#二四种互动模式)
3. [三种世界设定](#三三种世界设定)
4. [可以直接使用的提问示例](#四可以直接使用的提问示例)

### 第二部分：按版本使用

5. [版本选择](#五版本选择)
6. [版本一：普通聊天框版](#六版本一普通聊天框版)
7. [版本二：本地API网页版](#七版本二本地api网页版)
8. [版本三：完整Agent／研究版](#八版本三完整agent研究版)

### 第三部分：参考信息

9. [安全、隐私与史实边界](#九安全隐私与史实边界)
10. [常见问题](#十常见问题)

---

# 第一部分：功能说明

## 一、它可以做什么

韩琦智能体以历史人物资料为基础，但并不只适合讨论历史。你可以把它当作角色聊天对象、情境中的人物，也可以请它帮助分析现实问题。

| 你想做的事 | 可以怎样使用 |
|---|---|
| 随意聊天 | 聊日常、情绪、兴趣、关系或轻松话题 |
| 体验故事 | 设置历史、穿越、现代或架空场景 |
| 处理事情 | 请它分析问题、作出判断、安排步骤或起草文字 |
| 阅读材料 | 指定一组文献，请它依据材料查找、比较和回答 |

三个版本使用同一套人物设定。版本不同，只是打开方式和可读取资料的范围不同。

使用时请记住三个边界：

- 想让它依据文献研究时，需要先指定或上传材料；
- 联网检索默认关闭，只有用户主动打开或明确要求时才会使用；
- 情境和同人内容可以虚构，但不能把虚构内容说成真实史料。

## 二、四种互动模式

通常不需要手动选择模式，直接说出想做什么即可。需要更明确地控制回答时，也可以在开头写出模式名称。

### 2.1 闲谈

适合日常聊天、玩笑、情绪交流和人物关系互动。回答会尽量自然，不会主动写成长篇学术说明。

示例：

```text
韩相公，今天不谈公事，陪我聊聊你园子里最喜欢的景物。
```

### 2.2 情境

适合历史场景、穿越、架空和同人故事。可以出现虚构对白和动作；如果容易与史实混淆，智能体会说明这是情境演绎。

示例：

```text
情境模式。设定为嘉祐八年仁宗刚刚去世，请从韩琦视角处理眼前局面。
```

### 2.3 任事

适合把一件事交给它处理，例如项目决策、团队协作、风险判断、写作或复盘。回答会以解决问题为主，不会故意写成古文。

示例：

```text
任事模式。项目已经延期两个星期，请帮我判断是否继续上线，并给出明天可以执行的安排。
```

### 2.4 考据

适合依据指定材料进行研究。使用前需要告诉智能体材料在哪里，例如已上传文件、本地文件夹、平台知识库或明确的网址清单。

示例：

```text
根据我上传的《安阳集》、年谱和两篇论文，研究韩琦如何看待责任。请标出材料位置。
```

如果只说“请考据韩琦为什么反对青苗法”，却没有提供材料，智能体会先请你指定文献。这是为了避免假装查过实际并未读取的资料。

## 三、三种世界设定

世界设定决定韩琦身处什么时代、可以知道哪些事情。它可以与四种互动模式自由组合。

### 3.1 历史封闭

适合有明确历史年代的场景。韩琦只知道当时能够知道的事情，不会使用后来的历史结论或现代知识。

```text
历史封闭，时间是1029年。你刚入仕不久，我想问你现在最在意什么。
```

### 3.2 古今会谈

适合让韩琦来到现代与你交谈。面对公司、网络或人工智能等陌生事物，他会先听你解释，再作判断。

```text
古今会谈。我先向你解释社交媒体如何运作，再请你判断官员是否应该回应网络辱骂。
```

### 3.3 能力迁移

适合现实任务。智能体可以直接使用现代知识和表达方式，同时保留韩琦重视责任、事实、人员和执行的判断风格。

```text
能力迁移。请帮我处理一次职责不清、互相推诿的团队冲突。
```

## 四、可以直接使用的提问示例

### 轻松聊天

```text
韩相公，今天不许谈工作。你挑一个轻松的话题陪我聊。
```

### 历史情境

```text
历史封闭，设定为庆历三年。边事紧张，你刚收到一份互相矛盾的军报，会先做什么？
```

### 现代决策

```text
能力迁移。我们准备上线一个高风险功能，测试还不完整，但延期也会造成损失。请作出判断并安排后续步骤。
```

### 人际冲突

```text
我的合作者希望反复讨论，我想尽快行动。请帮我分析我们真正的分歧，并给出沟通方案。
```

### 同人演绎

```text
写一段仁宗与韩琦的雨夜谈话。说明这是情境演绎，但不要在正文中反复打断。
```

### 指定材料研究

```text
以我上传的三份材料为范围，比较它们对同一件事的说法，并告诉我哪些结论比较可靠。
```

---

# 第二部分：按版本使用

## 五、版本选择

如果不确定选哪一个，从普通聊天框版开始最省事。

| 你的情况 | 推荐版本 | 需要准备 |
|---|---|---|
| 想尽快体验，平时使用ChatGPT、Claude、豆包等聊天产品 | 普通聊天框版 | 一个支持上传文件的聊天产品 |
| 想在自己的电脑上打开独立网页，并选择模型服务 | 本地API网页版 | Python和模型API Key |
| 已经在使用Codex、Cursor、Claude Code、GitHub Copilot或Windsurf | 完整Agent／研究版 | 对应的Agent平台 |

简单判断：

- 只想聊天、写故事或处理一般问题：选普通聊天框版；
- 想在本地网页中添加文献、保留文献库：选本地API网页版；
- 想让Agent直接读取工作区文件或进行较完整的文献研究：选完整Agent／研究版。

API Key是模型服务提供的访问密钥，不是聊天产品的登录密码。只有本地API网页版需要自行准备模型API Key。

---

## 六、版本一：普通聊天框版

这是最容易开始的版本，不需要安装软件，也不需要使用命令行。

### 6.1 解压后需要用到的文件

打开 `han-qi-chat-pack-v1.0.zip`，主要会看到：

- `新手使用卡.md`：最短操作步骤；
- `启动提示词.md`：告诉聊天模型如何使用人物资料；
- `韩琦智能体-通用知识包.md`：韩琦的人物知识和行为设定。

### 6.2 最简单的使用方法

在支持上传文件的聊天产品中：

1. 新建一个对话。
2. 上传 `韩琦智能体-通用知识包.md`。
3. 打开 `启动提示词.md`，复制全部内容并发送。
4. 等模型回复后，直接开始聊天或交付任务。

第一句话可以很简单：

```text
韩相公，今天不谈国事，陪我随便聊聊。
```

也可以直接交付现实任务：

```text
项目已经延期，几个部门都说不是自己的责任。请你接手，给我一份明天可以开始执行的方案。
```

新建对话后，聊天产品可能不会继续保留之前上传的文件。如果人物表现突然变得普通，请重新上传知识包并发送启动提示词。

### 6.3 放进“项目”或“自定义智能体”长期使用

如果聊天产品提供“项目”“空间”“自定义智能体”“机器人”或“知识库”等功能，可以只设置一次：

1. 把 `启动提示词.md` 的内容放入“自定义说明”“系统指令”或名称相近的位置。
2. 把 `韩琦智能体-通用知识包.md` 上传到项目文件或知识库。
3. 如果看到“自动使用知识文件”“在回答中查找资料”等选项，请将它开启。
4. 保存后新建一个测试对话。

可以发送下面这句话检查是否设置成功：

```text
请只列出四种互动模式，不要扩展解释。
```

如果能够回答“闲谈、情境、任事、考据”，通常说明资料已经生效。

如果找不到上述设置入口，继续使用6.2节的一次性上传方法即可。

### 6.4 平台不能上传文件怎么办

可以只复制并发送 `启动提示词.md`，但人物细节和长期一致性会弱一些。

如果平台允许发送较长文字，也可以把启动提示词和知识包依次粘贴到同一对话中。内容过长时可能发送不完整，因此仍建议优先使用文件上传。

### 6.5 在普通聊天产品中研究材料

如果平台允许上传PDF或DOCX，可以先上传材料，再明确说明研究范围：

```text
只依据我刚才上传的三份文件回答。请注明文件名和相关位置；没有材料支持的地方请直接说明。
```

不同聊天产品读取附件的能力不同。如果回答中没有出现文件名或材料位置，不要直接认为它已经查阅了全部文件。

---

## 七、版本二：本地API网页版

这个版本会在你的电脑上打开一个独立聊天网页。示例配置当前选中DeepSeek API，只用于展示可运行的配置结构，不表示默认或推荐供应商。

### 7.1 开始前需要准备

- Windows、macOS或Linux电脑；
- Python 3.10或更高版本；
- 任一已配置模型服务的API Key；
- 能够访问该模型服务API的网络。

API Key需要从相应模型服务的开放平台申请，不能使用普通聊天网页的登录密码代替。

### 7.2 解压并创建个人配置

1. 解压 `han-qi-api-chat-v1.0.zip`。
2. 打开解压后的 `api-app` 文件夹。
3. 在这个文件夹中打开PowerShell、终端或命令提示符。

Windows PowerShell运行：

```powershell
Copy-Item ..\provider-config\providers.example.json ..\provider-config\providers.local.json
Copy-Item .env.example .env
```

macOS或Linux运行：

```bash
cp ../provider-config/providers.example.json ../provider-config/providers.local.json
cp .env.example .env
```

这两条命令只会生成你的个人配置文件，不会修改电脑的系统设置。

### 7.3 填写模型服务API Key

用记事本或其他文本编辑器打开 `api-app` 文件夹中的 `.env`，填写所选服务对应的一项。例如示例当前选中DeepSeek，可填写：

```dotenv
DEEPSEEK_API_KEY=
```

在等号后粘贴自己的密钥并保存：

```dotenv
DEEPSEEK_API_KEY=你的DeepSeek_API_Key
```

不要把 `.env` 上传给别人，也不要在截图中露出密钥。

### 7.4 启动网页

仍在 `api-app` 文件夹中运行：

```powershell
python server.py --config ..\provider-config\providers.local.json
```

浏览器通常会自动打开。如果没有自动打开，请访问：

```text
http://127.0.0.1:8765/
```

看到右上角显示“API Key 已配置”后即可开始使用。停止服务时，回到刚才的窗口按 `Ctrl+C`。

### 7.5 使用网页

- “互动模式”和“世界设定”第一次使用时保持“自动”即可；
- 在输入框中输入内容，按Enter发送，按Shift+Enter换行；
- 回答中的标题、列表、表格、引用、代码和链接会按富文本样式显示；
- 回答上方的“思考了几秒”可以展开，查看简要思考摘要或处理步骤；
- 回答下方只按文件名显示实际参考的本地文献，不会重复列出同一文件的多个片段；
- “使用本地文献库”决定是否参考已经添加的文献；
- “联网检索”默认关闭，打开后会持续使用，直到手动关闭或刷新页面；
- “清空会话”只清除当前聊天，不会删除文献库。

### 7.6 添加本地文献

在“本地文献库”区域：

- 点击“添加文件”可以选择一个或多个文件；
- 点击“添加文件夹”可以一次选择整个文件夹；
- 点击“查看文件”可以展开当前文献库的文件名、读取方式和大小；
- 点击“清空文献库”会删除已经添加的全部文献，操作前会再次确认。

支持TXT、Markdown、CSV、JSON、HTML、DOCX和PDF。单个文件不能超过25 MB，一次添加的文件总量不能超过50 MB。

如果要读取PDF，请先在 `api-app` 文件夹运行：

```powershell
python -m pip install -r requirements.txt
```

如果PDF是扫描图片，需要先用文字识别软件处理。

文献保存在你的电脑上。使用文献库回答时，提取文字约 2.4 万字以内的相关文献会整篇发送给当前模型服务；较长文献只发送与问题相关的部分。如果一次涉及多份文献，系统会自动控制总量，避免超出模型可处理范围。涉及私人或敏感内容时，可以关闭“使用本地文献库”。

“思考过程”显示的是模型提供的摘要，或网页根据文献检索、联网检索等可确认步骤整理出的简要过程，不会展示模型内部未公开的逐字推理。

### 7.7 使用联网检索

网页始终只显示一个“联网检索”开关，不需要选择具体搜索方式。

右上角显示“联网检索可用”时，打开开关即可。开关打开期间，每次提问都会联网查找并显示参考链接；关闭开关或刷新页面后停止联网。

使用本说明默认的 DeepSeek 配置时，联网检索直接使用同一个 `DEEPSEEK_API_KEY`，不需要另外申请联网密钥。填写 DeepSeek API Key 并重启服务后，右上角应显示“联网检索可用”。

如果改用其他模型服务，是否可用取决于该服务是否提供联网能力。如果开关不能使用，普通用户可以联系安装包提供方，不需要自行选择或配置搜索方式。

### 7.8 选择模型服务

`providers.example.json` 中的 `active_provider` 当前写为 `deepseek`，只是示例选择，不是默认推荐。文件同时提供以下典型服务名称和模型标识：

| `active_provider` | 服务平台 | 典型 `model` |
|---|---|---|
| `deepseek` | DeepSeek开放平台 | `deepseek-v4-flash` |
| `openai` | OpenAI API | `gpt-5.6-terra` |
| `qwen-bailian` | 阿里云百炼／通义千问 | `qwen3.7-plus` |
| `zhipu` | 智谱开放平台 | `glm-5` |
| `volcengine-ark` | 火山方舟／豆包 | `doubao-seed-2-0-lite-260215` |
| `custom` | 其他OpenAI兼容服务 | `provider-model-name`（占位） |

选择任一服务时：

1. 打开 `provider-config/providers.local.json`。
2. 将其中的 `active_provider` 改为想使用的服务名称。
3. 在 `.env` 中填写该服务对应的API Key。
4. 按该服务的官方说明核对 `base_url` 和 `model` 后再启动网页。百炼地址需要按地域和工作空间填写；火山方舟部分账户或自部署服务需要使用控制台生成的Endpoint ID。

修改后需要重新启动服务。表中名称只是典型示例，模型是否可用取决于地域、账户权限和供应商更新；应以服务商控制台及当前官方文档为准。

---

## 八、版本三：完整Agent／研究版

这个版本适合已经使用Codex、Cursor、Claude Code、GitHub Copilot或Windsurf的用户。它可以在获得权限后读取工作区文件，并按照同一套韩琦人物设定完成对话、任务和文献研究。

### 8.1 安装位置

解压 `han-qi-agent-public-v1.0.zip` 后，请复制整个 `han-qi-agent` 文件夹，不要只复制其中某一个文件。

| 平台 | 放入当前项目 | 放入个人目录 | 调用方式 |
|---|---|---|---|
| Codex | `.agents/skills/han-qi-agent` | `~/.agents/skills/han-qi-agent` | `$han-qi-agent` |
| Cursor | `.cursor/skills/han-qi-agent` | `~/.cursor/skills/han-qi-agent` | `/han-qi-agent` |
| Claude Code | `.claude/skills/han-qi-agent` | `~/.claude/skills/han-qi-agent` | `/han-qi-agent` |
| GitHub Copilot | `.github/skills/han-qi-agent` | `~/.copilot/skills/han-qi-agent` | 直接说明使用该技能 |
| Windsurf | `.windsurf/skills/han-qi-agent` | `~/.codeium/windsurf/skills/han-qi-agent` | `@han-qi-agent` |

“当前项目”表示只在这个项目中使用；“个人目录”表示希望在多个项目中使用。Windows中的 `~` 指你的用户文件夹。

复制完成后，重新启动对应平台或新建会话。

### 8.2 检查是否安装成功

新建会话后发送：

```text
请使用 han-qi-agent，只列出四种互动模式和三种世界设定，不要扩展解释。
```

如果回答正确，再发送一个简单任务：

```text
请使用 han-qi-agent，能力迁移模式，分析一个职责不清的团队问题。
```

如果平台没有自动使用技能，请采用表格中的调用方式明确指定。

### 8.3 在不同平台中调用

Codex示例：

```text
使用 $han-qi-agent，能力迁移模式，帮我处理一次团队责任不清的问题。
```

Cursor或Claude Code示例：

```text
/han-qi-agent 能力迁移模式，帮我审查这份项目复盘。
```

Windsurf示例：

```text
@han-qi-agent 使用能力迁移模式，替我判断这个项目是否应该延期。
```

GitHub Copilot示例：

```text
请使用 han-qi-agent 技能，分析当前项目的责任分工和发布风险。
```

### 8.4 使用本地文献

先把文献放入Agent能够读取的项目或文件夹，再说明材料范围和研究问题：

```text
使用 han-qi-agent，以“宋史资料/韩琦”文件夹为文献范围，研究韩琦与富弼关系的变化。请标出文件名和材料位置。
```

如果Agent提示没有权限：

- 把文献复制到当前项目；
- 在平台中允许读取该文件夹；
- 云端Agent需要先上传文件，不能直接读取你电脑上的任意路径。

只有在回答中出现实际文件名和材料位置时，才能确认它已经读取了文献。

### 8.5 使用联网检索

联网检索默认关闭。需要时请在当前任务中明确提出：

```text
请联网核实这个问题，并列出实际参考的网址。
```

仅选择“考据”或指定本地文献，不会自动联网。平台本身没有联网功能时，智能体会如实说明。

---

# 第三部分：参考信息

## 九、安全、隐私与史实边界

### 9.1 保护API Key

API Key相当于模型服务的付费凭证：

- 不要把它发送到聊天中；
- 不要上传或分享 `.env`；
- 截图、录屏或直播时不要露出密钥；
- 如果怀疑泄露，立即到模型服务平台撤销并重新创建。

### 9.2 哪些内容会发送给模型服务

普通聊天框版中，你的消息和上传文件会按照聊天产品的规则处理。

本地API网页版中：

- 聊天内容和人物知识会发送给你选择的模型服务；
- 打开“使用本地文献库”时，篇幅适中的相关文献会整篇发送，较长文献只发送相关部分；
- 打开“联网检索”时，当前问题会用于查找网页；
- 没有打开的功能不会自行启用。

“本地网页”表示网页运行在你的电脑上，不表示所有内容都只留在电脑中。只要使用在线模型，模型服务仍会收到回答所需的内容。

### 9.3 本机保存的内容

- 当前聊天只保存在浏览器页面中，刷新页面后会清空；
- 添加到本地文献库的文件不会因刷新页面而删除；
- “清空会话”只删除聊天；
- “清空文献库”才会删除已经添加的文献。

### 9.4 私人材料和版权

- 添加私人通信、身份证明、未公开稿件前，请先确认是否适合交给在线模型处理；
- 不要把API Key或密码放进文献库；
- 使用论文、电子书和档案图片时，请遵守来源的使用条件；
- 不要把没有授权的现代书籍或论文全文重新打包公开分享。

### 9.5 同人内容与真实历史

本项目允许创作虚构对白、动作、关系和场景，但不能把虚构内容说成真实史料。

如果需要沉浸式故事，可以要求智能体只在开头说明一次“情境演绎”；如果需要历史研究，请提供材料并要求标明出处。

## 十、常见问题

### 10.1 版本选择

**第一次使用，应该下载哪个包？**

下载 `han-qi-chat-pack-v1.0.zip`。它不需要安装软件，也不需要API Key。

**三个版本的人物设定一样吗？**

一样。选择版本只取决于你使用什么平台、是否需要本地文献和Agent功能。

### 10.2 普通聊天框版

**人物突然变得像普通聊天助手怎么办？**

新建对话后，重新上传知识包并发送启动提示词。还要确认没有同时使用相互冲突的角色设定。

**回答总是附带很长的史料说明怎么办？**

发送：

```text
当前是闲谈模式。除非我主动要求，否则不要追加考据说明。
```

**上传知识包后还是没有生效怎么办？**

发送“请只列出四种互动模式”。如果不能正确回答，重新上传文件；在项目或知识库中使用时，检查是否开启了使用知识文件的选项。

### 10.3 本地API网页版

**网页显示“API Key未配置”怎么办？**

检查 `api-app/.env` 中是否已经填写：

```dotenv
DEEPSEEK_API_KEY=你的密钥
```

保存后必须停止并重新启动服务。还要确认文件名是 `.env`，而不是 `.env.txt`。

**提示密钥错误或没有权限怎么办？**

确认使用的是DeepSeek开放平台提供的API Key，而不是聊天网页密码；如果密钥已经撤销、过期或余额不足，请到DeepSeek开放平台处理。

**网页打不开怎么办？**

确认启动服务的窗口仍然开着，然后在浏览器访问：

```text
http://127.0.0.1:8765/
```

如果终端提示找不到Python，请安装Python 3并重新打开终端。

**提示模型不存在或暂时不可用怎么办？**

模型名称可能发生变化。请查看DeepSeek官方更新记录，按说明更新 `providers.local.json`：

```text
https://api-docs.deepseek.com/updates/
```

**PDF无法读取怎么办？**

先在 `api-app` 文件夹运行：

```powershell
python -m pip install -r requirements.txt
```

如果PDF是扫描图片，还需要先进行文字识别。

**“联网检索”不能打开怎么办？**

使用默认的 DeepSeek 配置时，联网检索复用 `DEEPSEEK_API_KEY`，没有单独的联网密钥。请确认 DeepSeek API Key 已填写、模型配置使用 Responses API，并重新启动服务。改用其他模型服务后仍不能打开时，表示该服务没有提供可直接使用的联网能力，普通用户请联系安装包提供方。

**不希望文献内容发送给在线模型怎么办？**

关闭“使用本地文献库”。文件仍保存在你的电脑上，但当前问题不会查找或发送其中的内容。需要彻底删除时点击“清空文献库”。

**费用太高怎么办？**

缩短过长的对话，关闭暂时不需要的文献库和联网检索，或者在模型配置中选择费用较低的模型。实际费用请以模型服务平台显示为准。

### 10.4 完整Agent／研究版

**平台没有识别技能怎么办？**

- 确认复制的是完整 `han-qi-agent` 文件夹；
- 确认文件夹放在表格所列的位置；
- 重新启动平台或新建会话；
- 使用 `$han-qi-agent`、`/han-qi-agent` 或 `@han-qi-agent` 明确调用。

**Agent无法读取本地文献怎么办？**

把文献放入当前项目，或者在平台中允许读取相应文件夹。云端Agent需要先上传文件。

**要求考据时，为什么它先让我提供材料？**

考据模式只依据用户指定的材料。先提供文件、文件夹、知识库或网址范围，可以避免智能体假装查阅不存在的来源。

**文献回答不够准确怎么办？**

- 在问题中同时写出人名、官职、地点或年代；
- 尝试繁体字、简体字或常见异体字；
- 把一个大问题拆成几个小问题；
- 要求它查看相关段落的前后文；
- 要求列出文件名和材料位置，便于自己核对。

**网页会不会读完整篇论文？**

会。提取文字约 2.4 万字以内的相关文献默认整篇阅读，文件列表会显示“整篇阅读”。超过这一篇幅时显示“长文按需阅读”，回答时会查找与问题相关的部分。
