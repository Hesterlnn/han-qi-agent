# 复制本文件为 .env，只填写当前供应商对应的一项。
# .env 会由 server.py 自动读取，不要上传、分享或提交真实密钥。

DEEPSEEK_API_KEY=
OPENAI_API_KEY=
DASHSCOPE_API_KEY=
ZHIPU_API_KEY=
ARK_API_KEY=
HAN_QI_API_KEY=

# 使用 DeepSeek Responses API 时，联网检索直接复用 DEEPSEEK_API_KEY，无需额外密钥。
# 其他模型服务没有原生联网能力时，可由安装包提供方另行接入搜索服务；普通用户无需配置。
